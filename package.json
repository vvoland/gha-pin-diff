{
  "name": "gha-pin-diff",
  "version": "1.0.0",
  "description": "Comments on PRs with a diff summary when GitHub Actions are updated via SHA pinning",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "lint": "tsc --noEmit",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}

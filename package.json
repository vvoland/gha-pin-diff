{
  "name": "gha-pin-diff",
  "version": "1.0.0",
  "description": "Comments on PRs with a diff summary when GitHub Actions are updated via SHA pinning",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "gha-pin-diff": "./gha-pin-diff"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "lint": "tsc --noEmit",
    "start": "node dist/main.js",
    "local": "node dist/local.js"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^24.0.0"
  }
}
